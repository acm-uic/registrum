coverage:
  status:
    project:
      default: off
      client:
        flags: 
          - client
      api:
        flags: 
          - api
      banner:
        flags: 
          - banner
      banner-data:
        flags: 
          - banner-data

flags:
  api:
    paths:
      - api/
  client:
    paths:
      - client/
  banner:
    paths:
      - banner/
  banner-data:
    paths:
      - banner-data/
