coverage:
  status:
    project:
      default: off
      api:
        flags:
          - api
      banner:
        flags:
          - banner
      banner-data:
        flags:
          - banner-data

flags:
  api:
    paths:
      - api/
  banner:
    paths:
      - banner/
  banner-data:
    paths:
      - banner-data/
