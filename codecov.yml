coverage:
  status:
    project:
      default: off
      api:
        flags:
          - api
      banner:
        flags:
          - banner
      banner-data:
        flags:
          - banner-data
      common:
        flags:
          - common

flags:
  api:
    paths:
      - projects/api/
  banner:
    paths:
      - projects/banner/
  banner-data:
    paths:
      - projects/banner-data/
  common:
    paths:
      - projects/common/
