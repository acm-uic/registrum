coverage:
  status:
    project:
      default: off
      client:
        threshold: 0%
        flags:
          - client
      api:
        threshold: 0%
        flags:
          - api
      banner:
        threshold: 0%
        flags:
          - banner
      banner-data:
        threshold: 0%
        flags:
          - banner-data

flags:
  api:
    paths:
      - api/
  client:
    paths:
      - client/
  banner:
    paths:
      - banner/
  banner-data:
    paths:
      - banner-data/
